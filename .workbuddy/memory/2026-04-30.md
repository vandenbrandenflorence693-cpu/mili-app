# 2026-04-30 工作日志

## GitHub Pages 部署修复

### 完成的工作
1. 修复了 GitHub Pages 部署问题（`actions/deploy-pages@v4` 存在 bug）
2. 改用 `peaceiris/actions-gh-pages` action（但因 Fine-grained PAT 权限问题失败）
3. 最终通过 GitHub API 直接推送 dist 到 gh-pages 分支成功部署
4. GitHub Pages 配置为 `legacy` / `gh-pages` 分支模式

### 部署信息
- **仓库**: vandenbrandenflorence693-cpu/mili-app
- **网站地址**: https://vandenbrandenflorence693-cpu.github.io/mili-app/
- **Supabase**: https://vkdkarpfjyvwicjvrtwu.supabase.co
- **gh-pages 分支**: 有 index.html + assets/ (JS + CSS)

### 重要发现
- Fine-grained PAT 下 `github-actions[bot]` 推送权限受限
- `actions/deploy-pages@v4` 和 `peaceiris/actions-gh-pages` 都会因为权限问题失败
- 正确的部署方式：通过 GitHub API 用用户 PAT 直接推送 blob/tree/commit 到 gh-pages

### 部署脚本
- `d:/米粒/deploy_to_gh_pages.py` - 手动部署脚本（需要 GH_TOKEN 环境变量）
- 未来更新时：npm run build 后运行此脚本即可部署

### workflow 说明
- `.github/workflows/deploy.yml` 已更新但 workflow 部署（通过 GitHub Actions bot）目前不工作
- 手动部署流程：修改代码 → 本地 npm run build → python deploy_to_gh_pages.py
